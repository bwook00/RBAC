{
  "name": "rblayer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.3.11"
  },
  "workspaces": {
    "packages": ["apps/*", "bounded-contexts/*"],
    "catalogs": {
      "bun": {
        "@types/bun": "1.3.14",
        "@typescript/native-preview": "7.0.0-dev.20260517.1"
      }
    }
  },
  "scripts": {
    "type-check": "tsgo --noEmit",
    "lint": "biome ci --no-errors-on-unmatched",
    "lint:fix": "biome check --fix --no-errors-on-unmatched"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.15",
    "@types/bun": "1.3.14",
    "@typescript/native-preview": "catalog:bun"
  }
}
